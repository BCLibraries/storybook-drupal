sdc_storybook.generate_story:
  path: /sdc-storybook/generate-stories
  defaults:
    _controller: '\Drupal\sdc_storybook\Controller\ServerController::generateStories'
    _title: "Generate stories"
  requirements:
    _permission: "access content"
  options:
    no_cache: 'TRUE'
sdc_storybook.render_story:
  path: /storybook/story/{hash}
  defaults:
    _controller: '\Drupal\sdc_storybook\Controller\ServerController::renderStory'
    _title: "Render a Storybook story"
  requirements:
    _permission: "access content"
